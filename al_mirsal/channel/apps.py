"""App Configuration for al_mirsal.channel"""

from django.apps import AppConfig


class ChannelConfig(AppConfig):
    """al_mirsal.channel AppConfig"""

    name = "al_mirsal.channel"
    default_auto_field = "django.db.models.BigAutoField"
